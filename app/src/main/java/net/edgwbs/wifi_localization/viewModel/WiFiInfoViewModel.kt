package net.edgwbs.wifi_localization.viewModel

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class WiFiInfoViewModel(application: Application): AndroidViewModel(application) {
}